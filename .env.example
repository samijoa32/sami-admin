# 개발 환경: SQLite (파일 기반, 별도 설치 불필요)
DATABASE_URL="file:./dev.db"

# 운영 환경(Vercel) 배포 시에는 아래처럼 Postgres로 교체
# DATABASE_URL="postgresql://user:password@host:5432/dbname"

# 관리자 로그인 비밀번호 (직접 정하세요)
ADMIN_PASSWORD="sami1234"

# 배포 후 실제 도메인으로 교체
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
